export default class {
constructor(params) {
  this.params = params;
}

  // constructor() {
  //   this.setTitle(this.title);
  // }

  setTitle(title) {
    document.title = title;
  }

  async getHtml() {
    return "";
  }

}